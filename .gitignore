# Dependencies
node_modules/
.pnp
.pnp.js

# Build output
dist/
build/
*.tsbuildinfo

# Env / secrets (NUNCA versionar segredos ou chaves de biometria)
.env
.env.local
.env.*.local
*.pem
*.key

# Prisma
apps/api/prisma/*.db

# Coverage / testes
coverage/
.vitest/
# Playwright (E2E de UI)
playwright-report/
test-results/
.playwright/

# Logs
*.log
npm-debug.log*

# Editores / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# PWA / uploads locais (dados sensiveis nunca no repo)
apps/web/dev-dist/
uploads/
# Pasta de DADOS em runtime (STORAGE_LOCAL_PATH). Ancorada para NAO ignorar o
# codigo-fonte em apps/api/src/storage/ (regra ampla "storage/" fazia isso).
/apps/api/storage/

# Banco de desenvolvimento local (PostgreSQL embarcado)
.devdb/

# Capturas de tela geradas (reproduziveis via scripts/capturar-telas.mjs; o PDF
# de apresentacao ja as embute). Nao versionar imagens soltas.
docs/telas/
